# yaml-language-server: $schema=https://raw.githubusercontent.com/helm-unittest/helm-unittest/main/schema/helm-testsuite.json
suite: kyverno ArgoCD Application
templates:
  - templates/22-kyverno.yaml
tests:
  - it: should render no documents when disabled
    set:
      kyverno.enabled: false
    asserts:
      - hasDocuments:
          count: 0
  - it: should render one Application when enabled
    set:
      kyverno.enabled: true
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: Application
      - isAPIVersion:
          of: argoproj.io/v1alpha1
      - equal:
          path: metadata.name
          value: kyverno
      - equal:
          path: metadata.namespace
          value: control-plane-system
      - equal:
          path: metadata.annotations["argocd.argoproj.io/sync-wave"]
          value: "22"
      - equal:
          path: spec.source.path
          value: addon_charts/kyverno
      - equal:
          path: spec.destination.namespace
          value: kyverno
      - contains:
          path: metadata.finalizers
          content: resources-finalizer.argocd.argoproj.io
      - equal:
          path: spec.syncPolicy.automated.prune
          value: true
      - equal:
          path: spec.syncPolicy.automated.selfHeal
          value: true
      - contains:
          path: spec.syncPolicy.syncOptions
          content: CreateNamespace=true
      - contains:
          path: spec.syncPolicy.syncOptions
          content: PruneLast=true
      - equal:
          path: spec.syncPolicy.retry.limit
          value: 5
      - equal:
          path: spec.ignoreDifferences[0].kind
          value: ValidatingWebhookConfiguration
      - contains:
          path: spec.ignoreDifferences[0].managedFieldsManagers
          content: kyverno
      - equal:
          path: spec.ignoreDifferences[1].kind
          value: MutatingWebhookConfiguration
      - matchSnapshot:
          path: spec.syncPolicy
